---
apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: cilium
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "0"
  finalizers:
    - resources-finalizer.argocd.argoproj.io
  labels:
    app: cilium
spec:
  project: default
  syncPolicy:
    syncOptions:
      - CreateNamespace=true
    automated:
      prune: true
      selfHeal: true
  destination:
    name: in-cluster
    namespace: kube-system
  sources:
    - repoURL: https://helm.cilium.io/
      chart: cilium
      targetRevision: 1.19.3
      helm:
        values: |
          # -------------------------------------------------------------------
          # Hubble Relay & UI
          # -------------------------------------------------------------------
          hubble:
            relay:
              enabled: true
            ui:
              enabled: true

          # -------------------------------------------------------------------
          # Gateway API
          # -------------------------------------------------------------------
          gatewayAPI:
            enabled: true

          # -------------------------------------------------------------------
          # kube-proxy replacement & Devices
          # -------------------------------------------------------------------
          kubeProxyReplacement: true
          k8sServiceHost: "10.20.0.1"
          k8sServicePort: "6443"

          # Restrict NodePort lookup so Cilium does not intercept UDP 51820
          nodePort:
            range: "30000,32767"

          # Prevent host socket interception for non-pod host daemons
          hostServices:
            hostNamespaceOnly: true

          # -------------------------------------------------------------------
          # BPF & datapath
          # -------------------------------------------------------------------
          bpf:
            masquerade: true

          # -------------------------------------------------------------------
          # Host firewall — enables CiliumClusterwideNetworkPolicy for nodes
          # -------------------------------------------------------------------
          hostFirewall:
            enabled: true

          # -------------------------------------------------------------------
          # Metrics & monitoring
          # -------------------------------------------------------------------
          monitor:
            enabled: true

          # -------------------------------------------------------------------
          # Operator
          # -------------------------------------------------------------------
          operator:
            replicas: 1
            nodeGCInterval: 5m

          # -------------------------------------------------------------------
          # Miscellaneous
          # -------------------------------------------------------------------
          priorityClassName: system-cluster-critical

    - repoURL: https://github.com/TheRayquaza/home-cluster.git
      targetRevision: HEAD
      path: k8s/cilium
