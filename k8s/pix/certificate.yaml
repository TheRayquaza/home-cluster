---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: pix-tls-secret
  namespace: default
spec:
  secretName: pix-tls-secret
  issuerRef:
    name: letsencrypt-dns01
    kind: ClusterIssuer
  dnsNames:
    - pix.internal.rayq.app
