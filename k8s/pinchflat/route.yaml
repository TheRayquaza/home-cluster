
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: pinchflat-route
  namespace: pinchflat
spec:
  parentRefs:
    - group: gateway.networking.k8s.io
      kind: Gateway
      name: private-gateway
      namespace: default
      sectionName: pinchflat-https
  hostnames:
    - pinchflat.internal.rayq.app
  rules:
    - matches:
        - path:
            type: PathPrefix
            value: /
      backendRefs:
      - group: ''
        kind: Service
        name: pinchflat-service
        port: 8945
        weight: 1
