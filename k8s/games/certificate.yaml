---
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: games-tls-secret
  namespace: default
spec:
  secretName: games-tls-secret
  issuerRef:
    name: letsencrypt-dns01
    kind: ClusterIssuer
  dnsNames:
    - games.internal.rayq.app
