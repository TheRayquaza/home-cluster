apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: games-postgres-data
  namespace: games
spec:
  accessModes:
    - ReadWriteOnce
  storageClassName: local-path
  resources:
    requests:
      storage: 5Gi
