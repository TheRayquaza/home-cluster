name: Lint - kommande

on:
  pull_request:
    paths:
      - apps/kommande/**
      - .golangci.yml
      - .github/workflows/lint-kommande.yaml
      - .github/workflows/reusable-lint-go.yaml
  push:
    branches:
      - main
    paths:
      - apps/kommande/**
      - .golangci.yml
      - .github/workflows/lint-kommande.yaml
      - .github/workflows/reusable-lint-go.yaml

jobs:
  go:
    uses: ./.github/workflows/reusable-lint-go.yaml
    with:
      working-directory: apps/kommande
    permissions:
      contents: read
